version: "3.8"

services:
  backend-flask:
    build: ./backend-flask
    ports:
      - "4567:4567"
    volumes:
      - ./backend-flask:/backend-flask
    environment:
      # URLs
      FRONTEND_URL: "https://3000-${GITPOD_WORKSPACE_ID}.${GITPOD_WORKSPACE_CLUSTER_HOST}"
      BACKEND_URL: "https://4567-${GITPOD_WORKSPACE_ID}.${GITPOD_WORKSPACE_CLUSTER_HOST}"

      # Observability
      OTEL_SERVICE_NAME: "backend-flask"
      OTEL_EXPORTER_OTLP_ENDPOINT: "https://api.honeycomb.io"
      OTEL_EXPORTER_OTLP_HEADERS: "x-honeycomb-team=${HONEYCOMB_API_KEY}"

      # AWS + X-Ray
      AWS_XRAY_DAEMON_ADDRESS: "xray-daemon:2000"
      AWS_REGION: "eu-west-2"
      AWS_DEFAULT_REGION: "eu-west-2"
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY}"

      # Rollbar
      ROLLBAR_ACCESS_TOKEN: "${ROLLBAR_ACCESS_TOKEN}"

      # ✅ Cognito (backend must read these!)
      AWS_COGNITO_REGION: "eu-west-2"
      AWS_COGNITO_USERPOOL_ID: "eu-west-2_HKcHnrgiB"
      AWS_COGNITO_CLIENT_ID: "cte794vg6jirnr3eqtt2v500i"

  frontend-react-js:
    build: ./frontend-react-js
    ports:
      - "3000:3000"
    volumes:
      - ./frontend-react-js:/frontend-react-js
    environment:
      # Frontend → still uses REACT_APP_* names
      REACT_APP_BACKEND_URL: "https://4567-${GITPOD_WORKSPACE_ID}.${GITPOD_WORKSPACE_CLUSTER_HOST}"
      REACT_APP_AWS_PROJECT_REGION: "eu-west-2"
      REACT_APP_AWS_COGNITO_REGION: "eu-west-2"
      REACT_APP_AWS_USER_POOLS_ID: "eu-west-2_HKcHnrgiB"
      REACT_APP_CLIENT_ID: "cte794vg6jirnr3eqtt2v500i"

  xray-daemon:
    image: "amazon/aws-xray-daemon"
    command: ["xray", "-o"]
    ports:
      - "2000:2000/udp"
    environment:
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY}"
      AWS_REGION: "eu-west-2"
